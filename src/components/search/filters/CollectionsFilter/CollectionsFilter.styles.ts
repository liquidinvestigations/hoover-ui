import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles(() => ({
    checkbox: {
        padding: 5,
    },

    label: {
        margin: 0,
    },

    progress: {
        fontSize: '7.5pt',
    },

    docCount: {
        flex: '1 0 auto',
        paddingLeft: 6,
        textAlign: 'right',
    },
}))
